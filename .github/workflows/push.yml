name: Generate build number

on: push

jobs:
  build_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print new build number
        run: echo "BUILD_VERSION=${GITHUB_REF##*/}.${{ env.GITHUB_RUN_ID }}.${{ env.GITHUB_RUN_NUMBER }}"